#!/usr/bin/env bash
set -euo pipefail

# lib/network_install.sh
#
# Responsibility: Install network configuration, keeping PX4/tablet/WAN networks separated (Section 22, 40).
#
# Milestone 1 skeleton -- no implementation yet.
# Must never report success for work it did not perform.

network_install() {
  echo "network_install: would install network configuration"
  echo "network_install: not implemented"
  exit 1
}
