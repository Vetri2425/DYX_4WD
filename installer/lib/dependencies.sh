#!/usr/bin/env bash
set -euo pipefail

# lib/dependencies.sh
#
# Responsibility: Install apt dependencies (Section 40).
#
# Milestone 1 skeleton -- no implementation yet.
# Must never report success for work it did not perform.

dependencies() {
  echo "dependencies: would install apt dependencies"
  echo "dependencies: not implemented"
  exit 1
}
