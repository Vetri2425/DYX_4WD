#!/usr/bin/env bash
set -euo pipefail

# lib/common.sh
#
# Responsibility: Shared helper functions (logging, error handling) used by every other installer script.
#
# Milestone 1 skeleton -- no implementation yet.
# Must never report success for work it did not perform.

common() {
  echo "common: would provide shared helpers"
  echo "common: not implemented"
  exit 1
}
