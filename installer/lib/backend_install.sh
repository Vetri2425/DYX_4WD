#!/usr/bin/env bash
set -euo pipefail

# lib/backend_install.sh
#
# Responsibility: Install the backend Python environment (Section 40).
#
# Milestone 1 skeleton -- no implementation yet.
# Must never report success for work it did not perform.

backend_install() {
  echo "backend_install: would install the backend Python environment"
  echo "backend_install: not implemented"
  exit 1
}
