#!/usr/bin/env bash
set -euo pipefail

# lib/permissions.sh
#
# Responsibility: Set filesystem permissions, create service user/group if required (Section 40, 42).
#
# Milestone 1 skeleton -- no implementation yet.
# Must never report success for work it did not perform.

permissions() {
  echo "permissions: would set permissions and create service user/group"
  echo "permissions: not implemented"
  exit 1
}
