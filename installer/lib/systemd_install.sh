#!/usr/bin/env bash
set -euo pipefail

# lib/systemd_install.sh
#
# Responsibility: Install systemd units and enable the four dyx- services (Section 43).
#
# Milestone 1 skeleton -- no implementation yet.
# Must never report success for work it did not perform.

systemd_install() {
  echo "systemd_install: would install and enable systemd units"
  echo "systemd_install: not implemented"
  exit 1
}
