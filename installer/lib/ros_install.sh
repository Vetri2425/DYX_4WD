#!/usr/bin/env bash
set -euo pipefail

# lib/ros_install.sh
#
# Responsibility: Install ROS2 dependencies and build/install the ROS2 workspace (Section 40).
#
# Milestone 1 skeleton -- no implementation yet.
# Must never report success for work it did not perform.

ros_install() {
  echo "ros_install: would install ROS2 and build the workspace"
  echo "ros_install: not implemented"
  exit 1
}
