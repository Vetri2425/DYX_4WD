# DYX 4WD production release manifest
#
# Milestone 1 skeleton -- no release has been built yet.
# Section 49 (Release Artifact): will eventually list the contents of
# dyx-4wd-<version>-arm64.tar.zst (ROS2 install tree, backend package,
# installer, systemd files, default configuration, migration metadata,
# version manifest, checksums).
