# dyx_rpp production parameters
#
# Skeleton only -- Milestone 1. Values are intentionally left commented out;
# do not invent tuning numbers (per task instructions). Names below come
# directly from the architecture document where it enumerates them.
#
# Classes (Section 32):
#   LIVE             -- safe to change while the rover is running, applies immediately
#   IDLE_ONLY        -- changeable without ROS restart, but only outside an active mission
#   RESTART_REQUIRED -- infrastructure; requires a ROS restart to take effect
#
# ros2 param set /<node> <name> <value>   (Section 38) -- once implemented.

/**:
  ros__parameters:
    # Judgement call: Section 31 lists these 11 names for dyx_rpp without individually tagging each LIVE/IDLE_ONLY/RESTART_REQUIRED. All are classified LIVE here because Section 32's own LIVE examples are literally "RPP gains, lookahead, speed limits, yaw-rate limits, terminal thresholds, pivot tuning" -- i.e. this exact parameter set. Judgement call, flagged.
    # lookahead: ~   # LIVE
    # cross_track_gain: ~   # LIVE
    # heading_gain: ~   # LIVE
    # yaw_rate_gain: ~   # LIVE
    # yaw_rate_limit: ~   # LIVE
    # pivot_rate: ~   # LIVE
    # pivot_release_angle: ~   # LIVE
    # terminal_speed: ~   # LIVE
    # terminal_radius: ~   # LIVE
    # deceleration: ~   # LIVE
    # stop_settle_time: ~   # LIVE

