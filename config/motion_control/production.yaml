# dyx_motion_control production parameters
#
# Skeleton only -- Milestone 1. Values are intentionally left commented out;
# do not invent tuning numbers (per task instructions). Names below come
# directly from the architecture document where it enumerates them.
#
# Classes (Section 32):
#   LIVE             -- safe to change while the rover is running, applies immediately
#   IDLE_ONLY        -- changeable without ROS restart, but only outside an active mission
#   RESTART_REQUIRED -- infrastructure; requires a ROS restart to take effect
#
# ros2 param set /<node> <name> <value>   (Section 38) -- once implemented.

/**:
  ros__parameters:
    # DERIVED — NOT FROM V1 SPEC: Section 31 lists 6 names for dyx_motion_control; Section 32 does not give a motion_control-specific example list. max_speed/max_yaw_rate/max_acceleration/max_deceleration are classified LIVE (matching Section 32's "speed limits, yaw-rate limits" LIVE examples). command_timeout/telemetry_timeout are runtime safety/watchdog limits, not infrastructure configuration, so they are classified IDLE_ONLY per Section 32 ("some safety limits") and ADR-0009.
    # max_speed: ~   # LIVE
    # max_yaw_rate: ~   # LIVE
    # max_acceleration: ~   # LIVE
    # max_deceleration: ~   # LIVE
    # command_timeout: ~   # IDLE_ONLY
    # telemetry_timeout: ~   # IDLE_ONLY

