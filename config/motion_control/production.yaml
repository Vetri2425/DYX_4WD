# dyx_motion_control production parameters
#
# Skeleton only -- Milestone 1. Values are intentionally left commented out;
# do not invent tuning numbers (per task instructions). Names below come
# directly from the architecture document where it enumerates them.
#
# Classes (Section 32):
#   LIVE             -- safe to change while the rover is running, applies immediately
#   IDLE_ONLY        -- changeable without ROS restart, but only outside an active mission
#   RESTART_REQUIRED -- infrastructure; requires a ROS restart to take effect
#
# ros2 param set /<node> <name> <value>   (Section 38) -- once implemented.

/**:
  ros__parameters:
    # Judgement call: Section 31 lists 6 names for dyx_motion_control; Section 32 does not give a motion_control-specific example list. max_speed/max_yaw_rate/max_acceleration/max_deceleration are classified LIVE (they match Section 32's "speed limits, yaw-rate limits" LIVE examples). command_timeout/telemetry_timeout are classified RESTART_REQUIRED as a judgement call -- they read more like infrastructure/watchdog config than operator tuning, but the document does not say so explicitly. Flagged.
    # max_speed: ~   # LIVE
    # max_yaw_rate: ~   # LIVE
    # max_acceleration: ~   # LIVE
    # max_deceleration: ~   # LIVE
    # command_timeout: ~   # RESTART_REQUIRED
    # telemetry_timeout: ~   # RESTART_REQUIRED

